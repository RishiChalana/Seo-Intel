# LLM
GEMINI_API_KEY=

# Search provider (optional - falls back to MockSearchProvider if unset)
SERPAPI_KEY=

# Embedding backend: "tfidf" (default, free, local) or "voyage" (production quality)
EMBEDDING_BACKEND=tfidf
VOYAGE_API_KEY=
